# Port the server will listen on (Render/Railway will set PORT automatically)
PORT=4000

# Comma-separated list of allowed origins for CORS (use * to allow all in dev)
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend-domain.tld
